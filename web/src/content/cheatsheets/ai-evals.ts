import type { CheatSheetData } from "./types";

const aiEvals: CheatSheetData = {
  title: "The Ultimate AI Evals Cheat Sheet",
  subtitle: "Golden datasets · LLM-as-judge · RAG metrics · statistical rigor · regression testing",
  sections: [
    {
      title: "Core Concepts",
      color: "violet",
      rows: [
        { term: "Eval", desc: "Repeatable measurement of LLM system quality", code: "dataset + system_under_test + scorer\n-> aggregate score" },
        { term: "Golden dataset", desc: "Curated, versioned examples with verified expected output/rubric", code: "sample real traffic first\ntag by segment; commit to git" },
        { term: "Offline eval", desc: "Runs on a fixed dataset before deployment", code: "gates CI / release\ndeterministic, cheap to re-run" },
        { term: "Online eval", desc: "Runs on live production traffic", code: "sampled judge scoring\nimplicit signals: thumbs, regen rate" },
        { term: "Eval-driven development", desc: "Iterate prompts/models against a scored loop, not vibes", code: "change -> run eval -> compare\n-> ship winner -> monitor -> feed back" },
        { term: "Deterministic scorer", desc: "Exact/rule-based check, no LLM call needed", code: "exact_match(out, expected)\njson_schema_valid(out, Model)" },
        { term: "LLM-as-judge", desc: "Use a capable LLM to score/compare outputs via a rubric", code: "prompt = rubric + context + answer\ntemperature = 0, force JSON output" },
        { term: "Pointwise grading", desc: "Score one output in isolation against a rubric", code: "score 1-5 with numbered anchors\nharder for judge to calibrate absolutely" },
        { term: "Pairwise grading", desc: "Compare two outputs, pick the better one", code: "judge(question, a, b) -> 'A'|'B'|'tie'\nmore consistent than pointwise" },
        { term: "Regression testing", desc: "Re-run golden set on every prompt/model change", code: "fail build if score drops\nbeyond tolerance vs baseline" },
        { term: "Slicing", desc: "Report scores per segment, not just aggregate", code: "tag examples: billing, edge-case\nscore_by_slice(results)" },
        { term: "Judge calibration", desc: "Validate judge scores against human labels", code: "pearsonr(human_scores, judge_scores)\nre-check when judge model changes" },
      ],
    },
    {
      title: "Data Structures & Schemas",
      color: "blue",
      rows: [
        { term: "GoldenExample", desc: "One row of a golden dataset", code: "id, input, expected_output|rubric,\ntags: list[str], source: str" },
        { term: "Eval result row", desc: "Per-example scoring output, stored raw", code: "{id, output, score, error?}\nnever discard raw outputs" },
        { term: "Judge verdict schema", desc: "Force structured, parseable judge output", code: "{\"score\": int, \"reason\": str}\nfallback regex parse on failure" },
        { term: "Baseline file", desc: "Last known-good scores for regression diff", code: "baselines/latest_passing.json\nupdated only on merge to main" },
        { term: "RAGAS example schema", desc: "Question + answer + retrieved context triple", code: "{question, answer,\ncontexts: list[str], ground_truth}" },
        { term: "Confusion pair (McNemar)", desc: "Disagreement counts between two paired systems", code: "b = a_right_b_wrong\nc = a_wrong_b_right" },
        { term: "Rater label list", desc: "Parallel lists for kappa computation", code: "rater_a = ['yes','no',...]\nrater_b = ['yes','yes',...]" },
        { term: "Cheat: dataset split", desc: "Never tune and report on the same set", code: "val_set   -> pick best prompt\ntest_holdout -> report final number" },
      ],
    },
    {
      title: "Everyday Idioms",
      color: "emerald",
      rows: [
        { term: "Minimal eval loop", desc: "The shape every framework wraps", code: "for ex in dataset:\n    out = call_model(ex.input)\n    scores.append(scorer(out, ex))\nacc = sum(scores)/len(scores)" },
        { term: "Exact match", desc: "Strict equality for closed-answer tasks", code: "out.strip() == expected.strip()" },
        { term: "Schema validation scorer", desc: "For structured extraction tasks", code: "json.loads(out)\nPydanticModel.model_validate(data)" },
        { term: "Judge prompt anatomy", desc: "Rubric + anchors + forced JSON", code: "Context/Question/Answer\nScore 1-5 with anchors per level\nRespond with ONLY JSON" },
        { term: "Order-swap pairwise", desc: "Run comparison both directions", code: "judge(q, a, b)\njudge(q, b, a)  # must agree" },
        { term: "Faithfulness check", desc: "Are answer claims supported by context?", code: "decompose answer into claims\nask judge YES/NO per claim vs context" },
        { term: "Human vs judge agreement", desc: "Sanity-check a judge before trusting it", code: "pearsonr(human, judge)\nexact_agreement = mean(h==j)" },
        { term: "Wilson interval", desc: "Confidence interval for a pass rate", code: "successes=62, n=100\n-> approx (0.52, 0.71) at 95%" },
        { term: "Per-slice reporting", desc: "Never trust only the headline number", code: "defaultdict(list) by tag\nmean score per tag" },
      ],
    },
    {
      title: "Power Features",
      color: "amber",
      rows: [
        { term: "McNemar's test", desc: "Paired significance test on same examples", code: "stat = (abs(b-c)-1)**2 / (b+c)\nuse when comparing 2 systems, same set" },
        { term: "RAG metric quartet", desc: "RAGAS core metrics", code: "faithfulness, context_precision,\ncontext_recall, answer_relevance" },
        { term: "Bounded concurrency runner", desc: "Fast, safe eval execution", code: "semaphore = asyncio.Semaphore(8)\nasyncio.wait_for(call(), timeout=30)" },
        { term: "Retry with backoff", desc: "Don't let one flaky call sink a run", code: "for attempt in range(retries):\n    try: return await call()\n    except: sleep(2**attempt + jitter)" },
        { term: "Cohen's kappa", desc: "Chance-corrected inter-rater agreement", code: "(observed - expected)/(1 - expected)\n0.61-0.80 = substantial agreement" },
        { term: "Judge model tiering", desc: "Cheap judge on PR gate, strong judge nightly", code: "PR: small/cheap judge, single pass\nnightly: strong judge, pairwise+swap" },
        { term: "Sampling for scale", desc: "Judge a representative subset, not everything", code: "sample_rate = 0.05  # 5% of live traffic\nsize backed by confidence interval" },
        { term: "CI eval gate", desc: "Fail the build on real regression", code: "run fast subset -> compare baseline\nsignificance test, not raw delta" },
        { term: "Online sampled scoring", desc: "Judge a slice of live production traffic", code: "if random() < sample_rate:\n    score = judge(...); log_metric(score)" },
      ],
    },
    {
      title: "Pitfalls & Gotchas",
      color: "rose",
      rows: [
        { term: "Overfitting the eval set", desc: "Picking prompts against the same set you report on", code: "WRONG: tune + report on golden_v1\nRIGHT: tune on val, report on holdout" },
        { term: "Position bias", desc: "Judge favors whichever response shown first", code: "fix: swap order, require agreement\nor average both orders" },
        { term: "Verbosity bias", desc: "Judge rates longer answers as 'more thorough'", code: "fix: rubric explicitly penalizes\nunnecessary length" },
        { term: "Self-preference bias", desc: "Judge favors its own model family's style", code: "fix: use a judge from a\ndifferent model vendor/family" },
        { term: "Uncalibrated judge", desc: "Trusting judge scores with no human anchor", code: "fix: calibrate vs human labels\nre-check when judge model updates" },
        { term: "Aggregate-only reporting", desc: "Hides slice-level regressions", code: "95% overall can hide\n60% on 'refund requests' slice" },
        { term: "Ignoring noise in small deltas", desc: "62% vs 58% on 100 rows may be nothing", code: "check confidence interval /\nMcNemar's before declaring a winner" },
        { term: "BLEU/ROUGE as sole gate", desc: "Rewards lexical overlap, not correctness", code: "fine paraphrase -> low ROUGE\nwordy wrong answer -> inflated score" },
        { term: "Stale golden dataset", desc: "Never updated after initial build", code: "fix: feed production failures\nback into dataset continuously" },
        { term: "No timeout/retry in runner", desc: "One hung call stalls the whole CI job", code: "always: per-call timeout +\nbounded concurrency + retry" },
        { term: "PII in sampled golden data", desc: "Real traffic sampling without redaction", code: "redact/synthesize before\ncommitting to git-tracked dataset" },
        { term: "Silent model version bump", desc: "Provider updates model behind same API name", code: "fix: re-run full eval suite\non every model version pin change" },
      ],
    },
    {
      title: "Production Toolbelt",
      color: "cyan",
      rows: [
        { term: "OpenAI Evals", desc: "Framework/registry OpenAI uses for model evals", code: "open-source, Python\ngood for OpenAI-model-centric evals" },
        { term: "promptfoo", desc: "YAML-config CLI for prompt regression testing", code: "git-friendly, CI-integrated\nfast to adopt for prompt testing" },
        { term: "DeepEval", desc: "pytest-style framework with built-in metrics", code: "assert_test(input, output,\n  metrics=[FaithfulnessMetric()])" },
        { term: "RAGAS", desc: "Purpose-built RAG evaluation library", code: "evaluate(dataset,\n  metrics=[faithfulness, context_recall])" },
        { term: "Braintrust", desc: "Full eval + dataset + dashboard platform", code: "datasets, custom scorers,\ntrend dashboards, trace linkage" },
        { term: "LangSmith", desc: "LangChain's observability + eval platform", code: "datasets, annotation queues,\neval runs tied to traces" },
        { term: "Langfuse", desc: "Open-source-friendly tracing + evaluation", code: "self-hostable, scores API,\ndataset runs" },
        { term: "AI Harness (platform skill)", desc: "Orchestration/runner layer eval runners build on", code: "bounded concurrency, retries,\ntimeouts for LLM calls at scale" },
        { term: "CI integration pattern", desc: "Fast subset on PR, full suite nightly", code: "on: pull_request -> fast-eval\non: schedule -> full-eval + dashboard" },
        { term: "Related platform skills", desc: "Where evals connect", code: "Guardrails, Hallucination, RAG,\nPrompt Versioning, LLMOps" },
      ],
    },
  ],
};

export default aiEvals;
